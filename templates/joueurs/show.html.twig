{% extends 'base.html.twig' %}

{% block title %}Joueurs{% endblock %}

{% block body %}
    <h1>Joueurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ joueur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ joueur.nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ joueur.email }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ joueur.score }}</td>
            </tr>
            <tr>
                <th>PositionX</th>
                <td>{{ joueur.positionX }}</td>
            </tr>
            <tr>
                <th>PositionY</th>
                <td>{{ joueur.positionY }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('joueurs_index') }}">back to list</a>

    <a href="{{ path('joueurs_edit', {'id': joueur.id}) }}">edit</a>

    {{ include('joueurs/_delete_form.html.twig') }}
{% endblock %}
